NODE_ENV=development
PORT=3001

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=dmsoghwg_quanda
DB_USER=dmsoghwg_quanda_app
DB_PASSWORD=

# Generate a private random value with at least 32 bytes of entropy.
SESSION_SECRET=

# Separate private credential for protected analytics endpoints.
ADMIN_API_TOKEN=

# Comma-separated exact origins. Do not use * in production.
ALLOWED_ORIGINS=http://localhost:3000,https://quanda-dms4-demo-jet.vercel.app

SESSION_IDLE_MINUTES=30
AUTH_SESSION_DAYS=30
PASSWORD_HASH_ROUNDS=12
EVENT_BATCH_LIMIT=50
LOG_LEVEL=info
